# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile
#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi


export EDITOR=vim
set -o vi
PATH=/Users/tcasselman/Software/cityville_mobile/scripts:/usr/local/sbin:/usr/local/bin:~/dotfiles/bin:$PATH:/Developer/usr/bin/:/usr/local/mysql/bin


WEBAPP_DIR=~/Documents/workspace/.metadata/.plugins/org.eclipse.wst.server.core/tmp0/wtpwebapps/DatabaseInput/
GITREPO=git@github.com:Tylerc230/projects.git

VIMRUNTIME=~/.vim
export TERM=xterm-color
export CLICOLOR=1

#work
export SVNURL=https://svn.zynga.com/svn/cityville-mobile/project/
DEPLOYSRV=iphone-dev-01.ec2.zynga.com
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"

export DEV=citymobile-dev-01.ec2.zynga.com
export DONE=~/Dropbox/docs/done.txt
alias pd='pushd'
alias po='popd'

SVNSRV=https://svn.zynga.com/svn/cityville-mobile/server/
alias localize='php Server/game/scripts/compile_locale_xml.php'
[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm" # This loads RVM into a shell session.
STAGE=184.72.207.65
export TERM="xterm-256color"
